{
  "functions": {
    "api/chat.ts": {
      "maxDuration": 30
    }
  },
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "echo 'No build required for API functions'",
  "outputDirectory": "."
}